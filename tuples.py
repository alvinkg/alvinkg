# container is similar to lists and is also a container
coordinates = (4, 5)
# property is imutable, cannot be changed or modified
print(coordinates)
print(coordinates[1])
# tuples can be put into lists
